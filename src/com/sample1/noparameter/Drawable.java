package com.sample1.noparameter;
/* these eg are about implementing interface method
 * using lamda.
 * we dont create object when we use lamda. 
 * interface obj = ()->{
 * }
 * (argument-list) -> {body} 
 */
@FunctionalInterface
public interface Drawable {
	void draw();
	}
